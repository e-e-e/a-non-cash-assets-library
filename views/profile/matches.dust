{>"../base"/}
{<content}
<div class="inset-section">
{?error}<div class="error">{error}</div>{/error}
{?message}<div class="message">{message}</div>{/message}
<h2>Your Matches</h2>
{#user.have_matches}
	{>match your_have="true" /}
{/user.have_matches}
{#user.need_matches}
	{>match/}
{/user.need_matches}
<p><a href="/profile/add/have">List your non-cash assets</a> and 
<a href="/profile/add/need">the things you need</a> 
so we can find matches for you.</p>
</div>
{/content}
